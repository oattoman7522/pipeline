properties([parameters([choice(choices: ['Promote Vault HA', 'Synchronize Vault HA DRP'], name: 'template')])])

pipeline {
  agent any
  stages {
    stage('101') {
      steps {
        echo "Pran"
        echo "$template"
      }
    }
  }
} 
